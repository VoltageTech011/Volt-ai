{
  "name": "voltage-ai",
  "version": "1.0.0",
  "description": "Custom Self-Contained AI Model Backend & Frontend Engine",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "train": "python training/train.py",
    "export": "python training/export.py",
    "test:engine": "node -e \"const Engine = require('./src/engine/model'); async function run() { const e = new Engine(); await e.initialize(); const res = await e.generate('User: Who created you?\\nVoltage:'); console.log('\\nOutput:', res); } run();\""
  },
  "keywords": [
    "ai",
    "transformer",
    "onnx",
    "voltage",
    "custom-llm"
  ],
  "author": "Odunayo Ayinla (Voltage Lord)",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "onnxruntime-node": "^1.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
